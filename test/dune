(library
 (name ppx_test)
 (libraries dynt inspect)
 (inline_tests)
 (modules path ppx variantizer)
 (preprocess (pps ppx_expect dynt.ppx)))

(tests
  (names fuzzing)
  (libraries dynt)
  (modules fuzzing)
  (preprocess (pps ppx_expect dynt.ppx)))
