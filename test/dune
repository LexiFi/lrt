(library
  (name dynt_test)
  (libraries dynt)
  (inline_tests)
  (modules path ppx variantizer matcher)
  (preprocess (pps ppx_expect dynt.path dynt.deriving)))

(tests
  (names fuzzing)
  (libraries dynt)
  (modules fuzzing)
  (preprocess (pps ppx_expect dynt.path dynt.deriving)))
