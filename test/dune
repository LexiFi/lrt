(library
 (name ppx_test)
 (libraries dynt inspect)
 (inline_tests)
 (modules path ppx)
 (preprocess (pps ppx_expect dynt.deriving)))

(tests
  (names fuzzing)
  (libraries dynt)
  (modules fuzzing)
  (preprocess (pps ppx_expect dynt.deriving)))
