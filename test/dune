(library
 (name ppx_test)
 (libraries dynt inspect)
 (inline_tests)
 (modules path ppx)
 (preprocess (pps ppx_expect ppx_inline_test dynt.deriving)))

(tests
  (names fuzzing segfault)
  (libraries dynt)
  (modules fuzzing segfault)
  (preprocess (pps ppx_expect ppx_inline_test dynt.deriving)))
